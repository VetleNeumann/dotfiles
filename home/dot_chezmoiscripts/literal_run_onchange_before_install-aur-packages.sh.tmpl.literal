{{ $packages := list
	"paru" -}}

#!/bin/bash

set -euo pipefail

sudo pacman -S --needed base-devel
cd tmp
git clone https://aur.archlinux.org/paru.git
cd paru
makepkg -si

paru -S --needed - < {{ $packages | join " " }}
